const mutations = {
  SET_TOAST_VISIBLE (state, toastVisible) {
    state.toastVisible = toastVisible
  },
  SET_POP_VISIBLE (state, popVisible) {
    state.popVisible = popVisible
  }
}

export default mutations
